---
- name: Restart Titan service units
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    state: restarted
    daemon_reload: true
  loop:
    - titan-control-plane
    - titan-controller
    - titan-ai
    - titan-shop
  listen: Restart Titan services

